import { Database } from '@/database.types'

// Define the possible schema types
type SchemaType = 'production' | 'api'

export let row_tblProvidentInsuranceCoverTypes: Database[SchemaType]['Tables']['tblProvidentInsuranceCoverTypes']['Row']
